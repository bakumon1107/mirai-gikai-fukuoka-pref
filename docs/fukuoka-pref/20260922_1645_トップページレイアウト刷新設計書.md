# トップページレイアウト刷新 設計書（みらい議会＠福岡県）

作成日: 2026-09-22 ／ 最終更新: 2026-09-24

> 2026-09-24 更新(1): develop の `afc38c2` へ追従し、並行して進んでいた
> PR #44・#49・#51・#52 の内容を反映した（1.4節）。いずれもマージ済み。
> 会期日程が実データで取得できるようになったことに伴い 5.5 節を大幅に改訂し、
> 11章の論点1を解消した。
>
> 2026-09-24 更新(2): 配色と会期中モードのデザインが確定したため、
> **2章（カラートークン）を薄紫系で全面改訂**し、5.3節に会期中モードの
> 2段階を追加。5.8節にテーマタイルの検索語を確定値で記載。
> 11章の論点4・5・7を解消し、9章の実装ステップを組み直した。
>
> 2026-09-24 更新(3): テーマが **6→8分類**に変更されたため 5.8 節を改訂。
> 一般質問の既存11分類をすべて収める対応表が確定した（5.8.1節）。
> 11章の論点2は「対応表」から「全コンテンツへのタグ付与」へ論点が移った。
>
> 2026-09-24 更新(4): テーマタイルの下に「ほかのキーワードで探す」ボタンを追加
> （5.8.3節）。「その他」タイルは作らない方針で確定。
> ボタンの枠線色は参照デザインの `#cec8ea` がコントラスト 1.61:1 で不足するため、
> **`pref-accent` `#5a5591` に変更**（承認済み）。

デザイナー原案 [docs/fukuoka-pref/レイアウト変更/TOP_PAGE_REDESIGN.md](レイアウト変更/TOP_PAGE_REDESIGN.md)
を、現行コードベースの実態に突き合わせて実装可能な形に落とし込んだもの。
参照デザインは [top-desktop.html](レイアウト変更/top-desktop.html)（PC 1280px）と
[top-mobile.html](レイアウト変更/top-mobile.html)（スマホ 390px）。
これらはデザインツールの書き出しで `support.js` 参照や固定サイズ指定を含むため、
**見た目・文言・構造の参照**としてのみ扱う。

**本設計書のスコープは設計のみ。実装は承認後に別タスク・別PRで行う。**

### 原案との関係（実装者向けの注意）

原案 `TOP_PAGE_REDESIGN.md` は**デザイナーから受け取った状態のまま無改変で同梱**している。
出典として原文を保つためで、以下の2点は原案を読む際に読み替えが必要になる。
**食い違った場合は常に本設計書が正**。

| 原案の記述 | 実際 |
|---|---|
| 参照デザインのパスを `reference/top-desktop.html` / `reference/top-mobile.html` と表記 | `reference/` ディレクトリは存在しない。実ファイルは原案と同じ `docs/fukuoka-pref/レイアウト変更/` 直下（本書冒頭のリンクが正しい） |
| 参照デザイン `top-desktop.html` / `top-mobile.html` の配色（ティール系） | **不採用。** 2026-09-24 に配色が薄紫系へ確定した。**色は2章を正**とし、参照デザインからは構成・余白・文言だけを読む |

---

## 1. 背景と目的

現行トップは「テーマ別の議案カード」が主役になっている。しかし福岡県の議案は
本文がネットに公開されず情報が薄く、多くが工事請負契約など暮らしとの距離が遠い。
一方、知事記者会見（ほぼ隔週）・委員会（閉会中も開催）・一般質問は中身が豊富なのに、
トップでは小さなバナーリンクしかない。結果として、閉会中のトップは
「止まっているサイト」に見える。

目的は2つ。

1. **更新頻度の高い情報から順に並べ**、閉会中でも「動いている」サイトに見せる。
2. **お堅くない、気軽に見に来られる**トーンにする。

セクション順は更新頻度の高い順に
**知事会見 → 定例会 → 委員会 → 一般質問 → テーマ → 予算・議案**。
議案は個別カードを並べず件数の要約に留め、見出しや議題は「問いかけ」の言い回しにする。
定例会は「年4回の議会の本番」として位置づけを明示し、落としすぎない。

### 1.1 確定した方針

| 論点 | 決定 |
|---|---|
| スコープ | **設計書の作成のみ**。実装は別タスク |
| ナビ刷新（原案§4.1） | **PC横ナビ + スマホボトムナビを両方入れる** |
| 見出しフォント | **Zen Maru Gothic を weight 700 の1ウェイトのみ導入**し、**Lexend Giga は廃止** |
| テーマタイルのリンク先（5.8節） | **`/search?q=<検索語>`**。テーマページは作らない |
| 議案の件数の切り口（5.9.1節） | **審議状況（`status`）で出す**。参照デザインの内容分類（条例／工事の契約等）はDBに無いため不採用 |
| 配色（2章） | **現行サイトの薄紫系に揃える**。文字・塗りは `#5a5591`。緑（`#2aa693` / `#17705F`）は不採用 |
| 会期中モード（5.3節） | **段階1（文言差し替え）→ 段階2（ヒーロー差し替え）の2段階**で実装する |

### 1.2 デザインの確定について

配色と会期中モードは 2026-09-24 にデザインが届き、**すべて確定した**。
参照は `レイアウト変更/会期中モード 薄紫.dc.html`（薄紫・採用）と
`レイアウト変更/会期中モード.dc.html`（緑・不採用）。
原案 `TOP_PAGE_REDESIGN.md` も同日に更新され、§4.6（テーマ）・§4.8（会期中モード）・
§5（デザイントークン）が書き換わっている。

デザイン側で調査・確定された内容（本設計書もこれに従う）:

- 配色を薄紫系に確定。`#a495d6` / `#7a6cc0` は白文字とのコントラストが不足するため、
  文字・塗りには `#5a5591` を使う（2.2節で検算済み）
- 黄色を廃止し、紫＋ベージュの2系統に整理（紫と黄は補色でぶつかるため）
- テーマタイルの飛び先を `/search?q=` に確定し、**タイルごとの検索語まで決定**（5.8節）
- 検索対象に知事会見が含まれないことを確認し、テーマ見出しの補足文を変更（5.8節）

### 1.3 実装前に知っておくべき現状（調査結果）

手戻りの種になるため先に挙げる。詳細は各節で扱う。

1. **`#2aa693` はデザイントークン化されていない。** `@theme inline` にも `:root` にも
   存在せず、[web/src/app/layout.tsx](../../web/src/app/layout.tsx) の `themeColor`（67行目）と
   `NextTopLoader`（80行目）、および `web/public/icons/*.svg` の `fill="#2AA693"` に
   ハードコードされているだけ。**実際のUIアクセントは紫**
   （`--primary: #a495d6` / `--primary-accent: #7a6cc0`）。
2. **`#2aa693` の白文字コントラストは 3.01:1** で、文字色や塗りピルには使えない。
3. **PCナビ用の未使用コンポーネントが残っている。**
   `web/src/components/layouts/desktop-menu/`（8ファイル）はどこからも import されていない。
4. **議員テーブルはDBに存在しない**（`members` / `legislators` 系なし）。
5. **委員会の発言数は一覧用SELECTから意図的に除外されている**（6.2 節）。
6. **記者会見の詳細ページにアンカーが無い**（委員会側には `#topic-N` が実装済み）。
7. **原案§7.5 の「一般質問のリンクが r7-12 に固定」は現状のコードには存在しない。**
   `getLatestSessionWithQuestions()` で最新会期に動的追従済み。論点から落とす。

### 1.4 本設計書が前提とする、並行して進んでいる変更

初回の調査は develop の `db9cd51` 時点で行い、**現行の記述は更新履歴(1)で追従した
`afc38c2` を基準**にしている。以下は刷新の内容に直接影響する。

| 変更 | 状態 | 影響 |
|---|---|---|
| **PR #44** 代表質問・一般質問の区別 | **マージ済み**（2026-09-24） | `general_questions.question_type` カラム追加、`questionTypeLabel` / `questionMetaLabel` の新設、トップバナー文言変更。**5.7節の前提** |
| **#49** 注目の議案を公開済みのみに絞る | マージ済み | `findFeaturedBillsWithContents` に `publish_status = published` を追加。5.9節の前提 |
| **#51 / #52** 議案スクレイパーと会期日程パーサー | マージ済み | **`parseSessionSchedule()` により定例会の開会日・閉会日が実データで取れるようになった。5.5節の前提が大きく変わり、11章の論点1が解消した** |

いずれもマージ済みのため、本設計書の内容はそのまま着手できる。

---

## 2. デザイントークン設計

配色は**現行サイトの薄紫系に揃える**（原案§5、参照 `会期中モード 薄紫.dc.html`）。
参照デザイン `top-desktop.html` / `top-mobile.html` はティール時代の配色なので、
**色については本章を正**とし、構成・余白・文言のみをあちらから読む。

### 2.1 追加するカラートークン

`web/src/app/globals.css` の `@theme inline` に追加する。
名前空間は **`pref-*`** とする。

> **`--color-mirai-*` に足さないこと。** このリポジトリは本家 team-mirai の
> フォークで、`mirai-*` は上流由来の共通トークン。ここに県版固有の色を足すと
> 上流と同期したときに衝突する。県版で増やす色は `pref-*` に隔離する。
> （原案§5は「既存の命名 `--color-mirai-*` に揃える」としているが、
> フォーク運用の都合を優先してこちらを採る。）

**既存トークンで足りるもの（新設しない）**

| 用途 | 使うもの | 値 |
|---|---|---|
| ページ背景 | `--background` | `#f7f4ee` |
| ベージュ面（数字チップ・補助ピル） | `--color-mirai-surface-warm` | `#eae6dd` |
| テーマタイルのアイコン面（紫系） | `--color-mirai-gradient-start` | `#dbd3f9` |

**新設するトークン**

| トークン | 値 | 用途 |
|---|---|---|
| `--color-pref-accent` | `#5a5591` | 塗り・リンク・強調文字。**主役の紫** |
| `--color-pref-accent-hover` | `#474277` | hover |
| `--color-pref-accent-deep` | `#3d3868` | 淡色面の上に置く濃い文字 |
| `--color-pref-pill-bg` | `#cec8ea` | 選択中・状態ピルの背景（ナビ現在地、「いま定例会中」等） |
| `--color-pref-pill-text` | `#332e5e` | 同上の文字 |
| `--color-pref-hero` | `#eeebf8` | ヒーロー面 |
| `--color-pref-surface-tint` | `#f5f4fa` | サブ面・「いまここ」の面 |
| `--color-pref-surface-sub` | `#f6f4ee` | 定例会カード等のサブ面（ベージュ寄り） |
| `--color-pref-beige-text` | `#4a4336` | ベージュ面の上の文字 |
| `--color-pref-coral-bg` | `#fde2d6` | テーマタイルの差し色（アイコン面） |
| `--color-pref-coral-text` | `#7a2f14` | 同上の文字 |
| `--color-pref-coral-tint` | `#fff4f0` | 同上のタイル面 |
| `--color-pref-divider` | `#f0ece2` | 区切り線 |

本文は `#1f2a2a`、補足は `#3c4a48` / `#5b6563` を原案が指定しているが、
既存の `--color-mirai-text`（`#1f2937`）/ `--color-mirai-text-secondary`（`#404040`）で
代替でき、コントラストも既存側が有利なため**新設しない**。

### 2.2 配色の原則

原案§5 が定めた運用ルール。実装時のレビュー観点になる。

1. **紫のベタ塗り＋白文字は使わない。** 選択中・状態ピルは
   `pref-pill-bg`（淡い紫）＋ `pref-pill-text`（濃い紫）の組み合わせで表現する。
2. **黄色は使わない。** サイトの紫と黄は補色関係でぶつかるため、原案の黄系
   （`#FFE7A8` 等）は廃止し、**紫＋ベージュの2系統**にする。
   テーマタイルの差し色としてコーラルのみ加える。
3. **`#a495d6` / `#7a6cc0` は文字・塗りに使わない。** 白文字とのコントラストが
   それぞれ 2.68:1 / 4.44:1 で 4.5:1 に届かない（検算済み）。
   `#a495d6` は枠線・フォーカスリング等の装飾に限る。
   文字と塗りには `pref-accent`（`#5a5591`、白比 6.68:1）を使う。
4. **強調表示**（「つぎはここ」「いま開会中」「いまここ」）は
   枠 `pref-accent` 2px ＋ 面 `pref-surface-tint` で統一する。

### 2.3 コントラスト（全て検算済み・WCAG AA 4.5:1 基準）

| 前景 | 背景 | 比 |
|---|---|---|
| `pref-accent` `#5a5591` | 白 | 6.68:1 |
| `pref-accent` | `--background` `#f7f4ee` | 6.09:1 |
| `pref-accent` | `pref-surface-tint` `#f5f4fa` | 6.11:1 |
| `pref-accent` | `pref-hero` `#eeebf8` | 5.69:1 |
| `pref-accent` | `mirai-gradient-start` `#dbd3f9` | 4.68:1 |
| 白 | `pref-accent`（塗り） | 6.68:1 |
| `pref-accent-hover` `#474277` | 白 | 9.08:1 |
| `pref-accent-deep` `#3d3868` | `pref-hero` | 9.10:1 |
| `pref-accent-deep` | `pref-surface-tint` | 9.78:1 |
| `pref-accent-deep` | `mirai-gradient-start` | 7.48:1 |
| `pref-pill-text` `#332e5e` | `pref-pill-bg` `#cec8ea` | 7.72:1 |
| `pref-beige-text` `#4a4336` | `mirai-surface-warm` `#eae6dd` | 7.85:1 |
| `pref-coral-text` `#7a2f14` | `pref-coral-bg` `#fde2d6` | 7.59:1 |
| `mirai-text` | `pref-hero` | 12.56:1 |
| `mirai-text` | `pref-surface-tint` | 13.50:1 |
| `mirai-text` | `--background` | 13.44:1 |
| `mirai-text` | `pref-surface-sub` `#f6f4ee` | 13.42:1 |
| `pref-pill-text` `#332e5e` | 白（「ほかのキーワードで探す」の文字） | 12.40:1 |
| `pref-accent`（枠線） | 白（同ボタンの枠線） | 6.68:1 |

**唯一の禁止組み合わせ**

| 組み合わせ | 比 | 代替 |
|---|---|---|
| `pref-accent` `#5a5591` / `pref-pill-bg` `#cec8ea` | **4.16:1** | ピルの文字は `pref-pill-text` `#332e5e`（7.72:1）を使う |

緑案では禁止組み合わせが2つあったが、薄紫案は**1つだけ**で、
しかも原案がその組み合わせを指定していないため実害がない。

### 2.4 既存ページへの影響

`--primary: #a495d6` / `--primary-accent: #7a6cc0` / `.bg-mirai-gradient` は**据え置く**。
議案・予算・事務事業評価・チャットなど既存ページの見た目は変わらない。
`pref-*` はトップページの新セクション専用に使う。

`pref-accent`（`#5a5591`）は既存の `--primary`（`#a495d6`）を暗くした同系色なので、
トップと既存ページが並んでも系統は揃って見える。**緑案のような二色併存は起きない。**

なお `Button` / `Badge` の `default` variant は `.bg-mirai-gradient`（薄紫グラデ）だが、
本設計は影・グラデーションを使わない方針（2.5節）なので、トップの新セクションでは
`variant="outline"` かクラス上書きで `pref-*` に寄せる。実装時に共通ラッパを1つ作る。

**`meta theme-color` は `#2aa693`（旧ティールの名残）から `#a495d6` に更新する**
（原案§5）。`app/layout.tsx` の `viewport.themeColor`。
`NextTopLoader` の色も併せて `#5a5591` にする。

### 2.5 角丸・余白・影

| 対象 | 値 |
|---|---|
| ヒーロー・大ブロック | 28〜36px |
| カード | 22px |
| タイル | 18px |
| ピル | 999px（`rounded-full`） |
| セクション間 余白 | PC 40〜48px / スマホ 28〜32px |
| PC 左右マージン | 64px |
| タップ領域 | 最小 44×44px |

**影とグラデーションは使わない。** 面の区別は背景色と `pref-divider` の線で行う。
これは既存の `sm:shadow-lg`（`MainLayout`）や `.bg-mirai-gradient` と方向性が異なるため、
トップページでは `MainLayout` の shadow が付かない経路を通す必要がある（4.2節）。

---

## 3. フォント設計

### 3.1 Zen Maru Gothic の追加

`web/src/app/layout.tsx` に追加する。

```ts
import { Noto_Sans_JP, Zen_Maru_Gothic } from "next/font/google";

const zenMaruGothic = Zen_Maru_Gothic({
  variable: "--font-zen-maru-gothic",
  subsets: ["latin"],
  weight: ["700"],
});
```

`globals.css` の `@theme inline` に追加:

```css
--font-rounded: var(--font-zen-maru-gothic);
```

- **weight は `700` の1つだけ。** 参照デザインは見出しにほぼ 700 しか使っておらず、
  原案§5の 500 は実使用箇所がないため導入しない。
- `subsets` に `japanese` は**存在しない**（`next/font` の型定義で確認済み。
  `Zen_Maru_Gothic` が受け付けるのは `cyrillic | greek | latin | latin-ext`）。
  これは既存の `Noto_Sans_JP` と同じ状況で、**日本語グリフは preload されず
  unicode-range で遅延取得**される。つまり初期表示のクリティカルパスは太らない。
- `display` はデフォルトの `swap`。日本語見出しは一瞬 Noto Sans JP で描画されてから
  差し替わる（既存の日本語表示と同じ挙動）。

### 3.2 適用範囲（限定列挙）

`font-rounded` を当てるのは以下だけ。**本文・ナビ・ラベル・補足テキストは
Noto Sans JP のまま**にする（触れるグリフ数を抑え、ダウンロード量を減らすため）。

- h1（ヒーロー見出し）
- h2（各セクション見出し）
- h3（下段カードの見出し）
- 委員会カードの問いかけ見出し
- 定例会カードの「◯月」
- テーマタイルの漢字一文字アイコン

### 3.3 Lexend Giga の廃止

以下をすべて削除する。

- `web/src/app/layout.tsx` の `Lexend_Giga` import・定義・`body` の `variable` 付与
- `globals.css` の `--font-lexend: var(--font-lexend-giga);`
- `globals.css` `@layer components` の `.font-lexend { ... }`

根拠:

- 実使用は [web/src/components/top/hero.tsx](../../web/src/components/top/hero.tsx) の2箇所のみ。
  23行目は `siteConfig.features.showTeamMiraiSection` が `false` のため
  **空文字をレンダリングしている**。35行目は `Scroll` の10px表示。
- **この `Hero` コンポーネントは新デザインで丸ごと撤去される**（5.3節）ため、
  廃止後に参照は残らない。
- `subsets: ["latin"]` 指定により**5ウェイトが実際に preload されている**。
  削除はクリティカルパスの純粋な改善になる。

### 3.4 コスト見積もり

現状の実測値（`web/.next/static/media`）:

| 項目 | 値 |
|---|---|
| woff2 ファイル数 | 147 |
| 合計サイズ | 5.3MB（Noto Sans JP 3ウェイト + Lexend Giga 5ウェイト） |
| かな・ラテン等の軽いチャンク | 97ファイル / 平均 19KB |
| 漢字密集域のチャンク | 50ファイル / 平均 72KB |

Zen Maru Gothic 700 の追加でディスク上は **+約1.7MB**。
実ダウンロードは見出しが触れるチャンクのみで **概算 80〜200KB**、しかも遅延取得。
Lexend Giga 5ウェイトの削除で preload 対象が減るぶん、初期表示はむしろ改善が見込める。

---

## 4. ページ構成

### 4.1 セクション順

**PC（`pc:` = 1000px 以上、参照デザインは 1280px）**

1. ヘッダー（ロゴ / グローバルナビ / 検索）
2. ヒーロー：会期ステータスピル + 見出し + **最新の知事会見カード**（2カラム）
3. 定例会の帯（年4回、4枚横並び）
4. 左2/3：委員会の最新の話し合い（カード4枚 2×2）／右1/3：一般質問から（3件）
5. 気になるテーマから（8タイル横一列。幅が足りなければ4列×2行。5.8.3節）
6. 下段3カード：議員から探す（**今回は非表示**）／県のお金／議案の件数要約
7. フッター注記

**スマホ（`pc:` 未満、参照デザインは 390px）**

1. ヘッダー（ロゴ / 検索）
2. ヒーロー（見出し + 最新の知事会見カードを縦積み）
3. 定例会の帯（2×2）
4. 委員会の最新の話し合い（カード3枚 縦積み）+「委員会をぜんぶ見る」
5. 一般質問から（3件）
6. 気になるテーマから（3列×2行）
7. 小カード2枚：県のお金／議員から探す（**議員は非表示のため実質1枚**）
8. フッター注記
9. **ボトムナビ固定**（ホーム／会見／委員会／定例会／予算）

### 4.2 ブレークポイントとレイアウト幅

**このリポジトリの `md:` は 700px**（Tailwind 既定の 768px ではない）。
PC判定には独自の `pc:`（1000px）を使う。`globals.css` の定義:

```css
--breakpoint-sm: 500px;
--breakpoint-md: 700px;
--breakpoint-pc: 1000px;
--breakpoint-pcl: 1400px;
```

参照デザインの 1280px レイアウトは `pc:` 以上に適用し、
700〜1000px は中間レイアウト（2カラム → 1カラム、委員会カードは1列）として扱う。

**レイアウト幅の制約を外す方法**: 現在 `MainLayout` は通常ページを
`max-w-[700px]` に絞っている（[web/src/components/layouts/main-layout.tsx](../../web/src/components/layouts/main-layout.tsx) 34行目）。
新トップは PC で全幅を使うため、この制約を外す必要がある。

既存の逃がし口 `isWidePage()`
（[web/src/lib/page-layout-utils.ts](../../web/src/lib/page-layout-utils.ts) 27行目）が
`/jimu-jigyo` に対して `max-w-[1440px] mx-auto mt-24` を返しているので、
**ここに `/` を足すだけでよい**。新しい分岐は作らない。

```ts
export function isWidePage(pathname: string): boolean {
  if (pathname === "/") return true;
  return pathname.startsWith("/jimu-jigyo");
}
```

この経路は `sm:shadow-lg` が付かないため、2.5節の「影を使わない」方針とも一致する。

**副作用の確認が必要な点**: `isMainPage("/")` は `true` のままなので、
ヘッダーの `DifficultySelector` はトップに出続ける。新デザインのヘッダーに
難易度セレクタを残すかは実装時に判断し、残さないなら `isMainPage` ではなく
別の判定に切り替える（`isMainPage` は難易度セレクタとチャットサイドバーの
両方に効くため、安易に `/` を外さないこと）。

> **要注意 — チャットサイドバーのオフセットが失われる。**
> `MainLayout` の `isWide` 分岐は**早期 return** で、
> `useSidebarLayout`（`isMainPage(pathname) && siteConfig.features.aiChat`）による
> `pc:mr-[500px]` を**通らない**。
>
> ```tsx
> if (isWide) {
>   return <div className="relative max-w-[1440px] mx-auto mt-24">{children}</div>;
>   //     ↑ useSidebarLayout の余白が付かないまま返る
> }
> ```
>
> つまり `isWidePage` に `/` を足すと、**`aiChat` を有効にしたときに
> 固定表示の `ChatWindow`（右側 500px）とトップの本文が重なる**。
>
> 現在は `siteConfig.features.aiChat: false` のため `ChatWindow` 自体が
> 描画されず実害は出ないが、将来有効化した瞬間に壊れる。
> **`isWide` 分岐にもチャット幅ぶんの右余白を足せる構造にしておくこと。**
> 具体的には早期 return をやめて `cn()` で条件を合成し、
> `isWide && useSidebarLayout` のときに `pc:mr-[500px]` を付ける。
> 実装時にコメントを残す。

---

## 5. セクション別仕様

各節の「データ欠損時」は原案§9の受け入れ基準
「データが無い要素は空欄やプレースホルダを出さず非表示」に従う。

### 5.1 グローバルナビ（PC）

現状 `web/src/components/header/header-client.tsx` は全ブレークポイントで
ハンバーガー（Radix Popover）のみ。横並びナビは存在しない。

- `<nav>` 内に `hidden pc:flex` の横並びピルナビを追加する。
- ハンバーガーは `pc:hidden` にする。
- 現在ページは `bg-pref-pill-bg` + `text-pref-pill-text` のピル（7.72:1 ✓）。
  **紫のベタ塗り＋白文字は使わない**（2.2節の原則1）。
  非アクティブは `text-mirai-text`、hover で `bg-pref-surface-tint`。

| ラベル | リンク先 | 備考 |
|---|---|---|
| ホーム | `/` | |
| 質問 | `/questions` | 最新会期へ redirect される既存ルート。PR #44 で対象が「代表質問・一般質問」に広がるため、ピルに収まる短縮形にする（5.7節） |
| 委員会 | `/committees` | |
| 議案 | `/sessions` | 議案一覧の入口。専用ルートが無いため定例会一覧を当てる |
| 知事会見 | `/press-conferences` | |
| 予算 | `/budget` | |

**未使用コンポーネントの扱い**: `web/src/components/layouts/desktop-menu/`（8ファイル）は
どこからも import されていない。現状の `links.tsx` は構成が異なり流用の旨味が薄いため、
**横並びナビは新規作成し、`desktop-menu/` は別PRで削除する**ことを推奨する。

### 5.2 ボトムナビ（スマホ）

- 高さ 72px、5等分グリッド、lucide アイコン + ラベル。**インラインSVG禁止**。
- `pc:hidden` で PC では非表示。
- `env(safe-area-inset-bottom)` を `padding-bottom` に加算する。
- `(main)/layout.tsx` の `<main>` に下余白（`pb-[calc(72px+env(safe-area-inset-bottom))] pc:pb-0`）を
  足し、コンテンツ末尾がナビに隠れないようにする。
- 5枠: ホーム／会見／委員会／定例会／予算。

**PCナビとの差は意図的**。PC は「一般質問」「議案」を独立項目に持つが、
スマホは5枠に収めるため、議案と一般質問を「定例会」タブ
（定例会一覧 → 各定例会の議案・一般質問）に畳む。

**既存のチャットFABとの衝突**: `web/src/features/chat/client/components/chat-button.tsx` 115行目が
`fixed ... bottom-4 ... pc:hidden` で同じ位置に出る。
現状は `siteConfig.features.aiChat: false` のため描画されないが、
将来有効化する際はFABを `bottom-[88px]` 相当へ逃がす必要がある。実装時にコメントを残すこと。

### 5.3 ヒーロー

現行の `Hero`（背景画像 `/img/hero_background.png` + Scroll インジケーター）と
`CurrentCouncilSession`（「本日は 開会中/閉会中」）は**撤去**し、ここに統合する。

会期状態で表示が変わる。判定は既存の `getCurrentCouncilSession(getJapanTime())` を流用し、
`null` なら閉会中、そうでなければ会期中として `closed | in_session` を返す。

面は `bg-pref-hero`、角丸 36px（スマホ 28px）。

#### 5.3.1 閉会中モード

| 要素 | 内容 | データ源 | 欠損時 |
|---|---|---|---|
| 会期ステータスピル | 「いまは閉会中」 | 上記の判定 | 常に表示 |
| 次の定例会ピル | 「次の定例会 ◯月◯日から」 | `council_sessions.start_date`（5.5.1節） | 日程が無ければ**ピルごと非表示** |
| 見出し | 「福岡県、いま何が話されてる？」 | 固定文言 | — |
| 説明文 | 「知事の記者会見は、だいたい2週に1回。県がいま力を入れていることが、いちばん早くわかります。」 | 固定文言 | — |
| 知事会見カード | 5.4節 | | |

PC は2カラム（左：ピル+見出し+説明、右：会見カード）、スマホは縦積み。

#### 5.3.2 会期中モード

**公開時点で会期中の公算が高い**（令和8年9月定例会は 2026-09-09〜10-16）。
閉会中だけ実装すると「いまは閉会中」という誤表示になるため、原案§4.8 のとおり
**2段階**で進める。段階1だけでも公開に耐える。

**段階1（公開に必須）: 文言差し替え**

レイアウトは閉会中と共通のまま、次だけ差し替える。

| 要素 | 会期中の表示 |
|---|---|
| 会期ステータスピル | 「いま定例会中」（背景 `pref-pill-bg` / 文字 `pref-pill-text` / 太字） |
| 2つ目のピル | 「◯月定例会 ◯月◯日まで」（スマホは「◯月◯日まで」） |
| 定例会の帯 | 5.5節の会期中ルールに従う |
| スマホの委員会セクションの補足文 | 「閉会中も、テーマごとに…」は非表示、または「定例会のあいだは、議案を委員会でくわしく審査します」に差し替え |

**段階2: ヒーローを定例会に差し替え**

参照 `レイアウト変更/会期中モード 薄紫.dc.html`。

| 要素 | 内容 | データ源 | 欠損時 |
|---|---|---|---|
| ピル2つ | 「いま定例会中」＋会期（「9月9日〜10月16日」） | `start_date` / `end_date` | — |
| 見出し | 「◯月定例会、いま何を決めてる？」 | 会期名から月を取る | — |
| 説明文 | 「年4回の議会の本番。補正予算や条例は、この期間に採決されます。」 | 固定文言 | — |
| 進み具合バー | 「N日目／全M日」 | `start_date` / `end_date` から算出（5.3.3節） | — |
| 会期の流れ | 4ステップのタイムライン（5.3.3節） | 会期日程ページ | **行ごと非表示**。開会・閉会の2行だけでも成立させる |
| 入口タイル2枚 | 「今回の議案 N件を審議中」→ `/sessions/[id]/bills`／「一般質問 N人が質問」→ `/sessions/[id]/questions` | 5.9.1節の `status` 集計／`general_questions` の件数 | 0件・未取得なら**タイルごと非表示** |
| 知事会見 | **ヒーロー直下に1行の帯**（日付＋先頭の話題＋「ほか◯件」＋「会見を見る」） | 5.4節 | 非表示 |

**会期中は知事会見がヒーローの主役から降りる。** 閉会中は会見が右カラムの主役だが、
会期中は定例会に譲って1行の帯になる。セクションの優先順位そのものが変わる点に注意。

#### 5.3.3 「会期の流れ」と進み具合の算出

**進み具合**は `start_date` / `end_date` だけで出せる。
令和8年9月定例会なら 9/9〜10/16 で全38日、9/24 時点で16日目。
日数は**両端を含めて数える**（9/9 が1日目）。

**会期の流れ**は4ステップ。

| ステップ | ラベル | 令和8年9月定例会での実日程 |
|---|---|---|
| 1 | 開会・議案の説明 | 9月9日 |
| 2 | 一般質問・代表質問 | 9月15日〜9月25日 |
| 3 | 委員会でくわしく審査 | 9月28日〜9月30日（常任委員会） |
| 4 | 採決・閉会 | **議案採決 10月1日 ／ 閉会 10月16日** |

各行に日付を出し、終わった段階には「おわった」タグ、現在の段階には「いまここ」
（面 `pref-surface-tint`）を付ける。

> **重要 — 採決日と閉会日は別の日**。
> 参照デザインは4ステップ目を「10月16日 採決・閉会」と1行にまとめているが、
> これは**デザイン上の参考表記**であり実日程とは異なる。
> 実際には議案の採決は 10月1日（常任委員長報告・採決）に終わり、
> その後は決算特別委員会が続いて 10月16日 に閉会する。
> **「議案がいつ決まるか」を伝えるのが目的なので、採決日は採決日として出すこと。**
> ステップを5つに割るか、4ステップ目に両方の日付を併記する。
> デザインの日付をそのまま実装しないこと。

**日程データの取得**: 会期日程ページには各日の議事日程（`開会` / `代表質問` /
`一般質問` / `常任委員会` / `議案採決` / `閉会` 等）が日付付きで載っており、
ステップ2〜4を機械的に判定できる。ただし現在の `parseSessionSchedule()` は
**開会日と閉会日しか抽出していない**（5.5.1節）。
段階2を実装するときにパーサーを拡張する。

拡張しない場合でも、原案の指示どおり**行ごと非表示**にすれば開会・閉会の2行で成立する。
段階2の着手判断とパーサー拡張はセットで考える。

### 5.4 知事会見カード

| 要素 | データ源 | 欠損時 |
|---|---|---|
| バッジ「知事会見」 | 固定 | — |
| 日付（曜日付き） | `press_conferences.held_at` | — |
| 「最新」タグ | 固定 | — |
| 話題リスト（最大4件・番号付き） | `press_conference_items`（`order_index` 昇順） | 0件ならリストごと非表示 |
| 過去3回の日付チップ | 直近3件の `held_at` / `slug` | 3件未満ならある分だけ |
| 「ぜんぶ見る」 | `/press-conferences` | — |

最新1件は既存の `getLatestPressConference()` をそのまま使える
（`press_conference_items` と `press_conference_turns` までネストで取得済み）。

**話題の文言**: 原案は AI生成の `headline_short`（全角20字前後の問いかけ）を
想定しているが未生成。**当面は `press_conference_items.title` をそのまま表示する**
（7章のフォールバック方針）。

**過去3回の日付チップ用に軽量な repository 関数を新設する**:

```ts
// web/src/features/press-conferences/server/repositories/press-conference-repository.ts
export async function findRecentPressConferenceDates(
  limit: number
): Promise<{ id: string; slug: string; heldAt: string }[]>
```

既存の `getPressConferences()` は `*, press_conference_items(*, press_conference_turns(*))`
で**全会見の全発言本文まで引いてくる**ため、日付3件の用途には過剰。流用しない。

**詳細ページへのアンカー**: 現在 `press-conference-detail.tsx` /
`announcement-item.tsx` / `qa-item.tsx` のいずれにも `id=` 属性がない。
委員会側は `#topic-${topic.topicOrder}` が実装済み
（[web/src/features/committee-minutes/server/components/meeting-detail-view.tsx](../../web/src/features/committee-minutes/server/components/meeting-detail-view.tsx) 97行目）なので、
**同じパターンを移植する**。

- 各 item に `id={`item-${item.id}`}` を付与
- トップからのリンクは `/press-conferences/[slug]#item-[id]`
- QA はアコーディオン（`QaItem defaultOpen={i === 0}`）なので、
  ハッシュが自分を指すときに開く制御を追加する

アンカーが未実装の間は、カード全体を `/press-conferences/[slug]` に向ければよい。

### 5.5 定例会の帯

見出し「定例会は年4回。議会の本番です」／
補足「議案の採決と一般質問は定例会で。あいだの期間は委員会が開かれます。」

当年の定例会4回（2月・6月・9月・12月）をカード表示。状態は日付から算出する。

| 状態 | 条件 | 表示 |
|---|---|---|
| `おわった` | `end_date` が今日より前 | リンク「議案・一般質問を見る」等 |
| `いま開会中` | `start_date <= 今日 <= end_date` | 枠 `pref-accent` 2px + 面 `pref-surface-tint` で強調。タグは背景 `pref-pill-bg` / 文字 `pref-pill-text`。説明文は「◯◯を審議中」、下段は「◯月◯日まで」 |
| `つぎはここ` | 次に開会する回 | `いま開会中` と同じ強調スタイル。開会日を表示 |
| `これから` | それ以降 | 前年同月に一般質問データがあれば「昨年の◯月を見る」 |

> **会期中は強調を「開会中の回」だけにする**（原案§4.8）。
> 次の回に `つぎはここ` を併記せず `これから` 扱いにする。
> 強調枠が2つ並ぶと、どちらが「いま」なのか分からなくなるため。
> 閉会中は `つぎはここ` が唯一の強調になる。つまり**強調枠は常に1つだけ**。

**`getAllPastSessions()` は使えない。** repository の実装が
「`is_active = false` かつ published な議案を1件以上持つ会期」に絞っているため、
「一般質問はあるが議案未投入」の会期が落ちる。
**当年の全 `council_sessions` を返す新規 repository 関数を設ける。**

```ts
export async function findCouncilSessionsByYear(
  year: number
): Promise<CouncilSession[]>
```

**状態算出は純粋関数に切り出し、テストを必須とする**（CLAUDE.md 規約）:

- `web/src/features/council-sessions/shared/utils/resolve-session-slot-status.ts`
- `resolve-session-slot-status.test.ts`（同階層）

各月の一行説明（2月＝「新しい年度の予算を決める」など）は固定文言としてここに定義する。

#### 5.5.1 日程データは実データで取れるようになった（#52）

原案§7.1 および本書11章の論点1「次の定例会の日程データの持ち方」は、
**#52 で解消した**。`packages/seed/fukuoka/parse-session-schedule.ts` の
`parseSessionSchedule(html)` が県議会の会期日程ページから開会日・閉会日を抽出し、
`scrape-bills.ts` が `startDate` / `endDate` として出力する。
会期が年をまたぐケース（12月定例会）にも対応済み。

スクレイプ済みの実データ（`docs/data/bills/*.json`）:

| 会期 | 開会 | 閉会 |
|---|---|---|
| 令和7年2月定例会 | 2025-02-04 | 2025-02-20 |
| 令和7年4月臨時会 | 2025-04-11 | 2025-04-11 |
| 令和7年5月臨時会 | 2025-05-16 | 2025-05-16 |
| 令和7年6月定例会 | 2025-06-10 | 2025-07-04 |
| 令和7年9月定例会 | 2025-09-08 | 2025-10-14 |
| 令和7年12月定例会 | 2025-12-01 | 2025-12-19 |
| 令和8年2月定例会 | 2026-02-20 | 2026-03-24 |
| 令和8年6月定例会 | 2026-06-08 | 2026-06-25 |
| 令和8年8月臨時会 | 2026-08-17 | 2026-08-17 |
| 令和8年9月定例会 | 2026-09-09 | 2026-10-16 |

これで原案の注記「実際の月・日程は公式日程で確認すること」に答えが出た。
**定例会は 2月・6月・9月・12月の年4回で正しい。**

**ただし2点、実装に効く事実がある。**

1. **臨時会が混ざる。** 令和7年は4月・5月、令和8年は8月に臨時会がある。
   `council_sessions` にはこれらも入るため、定例会の帯は
   **会期名に「定例会」を含むものだけに絞る**こと。`findCouncilSessionsByYear` の
   戻り値をそのまま4枚に流し込むと臨時会が混入する。
   絞り込みは純粋関数側（`resolve-session-slot-status.ts`）に置き、テストで臨時会を除外する
   ケースを必ず書く。
2. **令和8年12月定例会の日程はまだ公式に未掲載。** スクレイプ対象に存在しない。
   つまり `これから` 状態のカードは**日程が無い状態で描画される**のが通常であり、
   7章のフォールバック（開会日を出さない）が実際に効く。

#### 5.5.2 未実装：スクレイプした日程をDBへ入れる経路

`packages/seed/fukuoka/seed-bills.ts` は `council_sessions` を**会期名で引くだけ**で、
`start_date` / `end_date` を書き込まない。
つまり JSON には正しい日程があるが、**DBに反映する経路が無い**。

定例会の帯は `council_sessions.start_date` / `end_date` に依存するため、
**この投入経路の追加が 5.5 節の前提条件**になる。9章の PR1（データ層）に含める。

- `seed-bills.ts` に `start_date` / `end_date` の upsert を足すか、
  会期専用の `seed-council-sessions.ts` を分けるかは実装時に判断する。
- 既存レコードの日程を上書きすることになるため、**`--dry-run` で差分を確認してから適用**し、
  CLAUDE.md のDB更新ルールに従ってユーザー確認を取る。

### 5.6 委員会の最新の話し合い

表示件数は PC 4件 / スマホ 3件。

**選定ルール**

1. 委員会（`committee_slug`）ごとに最新の開催回を取る（1委員会1枚）。
2. **発言数が10未満の回は除外**し、その委員会の一つ前の回を候補にする。
3. 開催日の新しい順に並べて上位N件。

**カードの項目**

| 要素 | データ源 | 欠損時 |
|---|---|---|
| 委員会名 / 開催日 | `committee_meetings.committee_name` / `meeting_date` | — |
| 問いかけ見出し | AI生成 `headline_question`（未生成）→ **`committee_meeting_topics[0].title` で代替** | — |
| 数字チップ | AI生成 `highlight`（未生成） | **チップごと非表示** |
| 要約（1〜2文・全角80字程度） | AI生成 `summary_short`（未生成）→ **`committee_meetings.summary` の切り詰めで代替** | 非表示 |
| 「発言のやり取りを読む（N発言）」 | 発言数（下記） | 数値が無ければ「発言のやり取りを読む」 |

リンク先はカード全体が `/committees/[slug]/[documentId]`、
発言リンクが `/committees/[slug]/[documentId]/transcript`。
下に「委員会をぜんぶ見る」（`/committees`）。

#### 5.6.1 発言数の取得（要設計判断）

発言は独立テーブルではなく `committee_meetings.speeches`（jsonb 配列）に入っている。
一覧用の `LIST_SELECT`
（[web/src/features/committee-minutes/server/repositories/committee-meeting-repository.ts](../../web/src/features/committee-minutes/server/repositories/committee-meeting-repository.ts) 75〜85行目）は
**`speeches` を意図的に除外している**。選定ルール2と「N発言」表示の両方がこれに依存する。

| 案 | 内容 | 評価 |
|---|---|---|
| **A（推奨）** | `committee_meetings` に生成列 `speech_count int generated always as (jsonb_array_length(speeches)) stored` を追加する migration を打ち、`LIST_SELECT` に加える | `speeches` 本体を一覧に載せない現方針を維持でき、ソート・フィルタもSQLで完結。`speeches` が null のケースに `coalesce` が要る |
| B | `jsonb_array_length` を返す View / RPC を新設 | migration は同等に必要で、型生成と呼び出しが増えるぶん複雑 |
| C | `LIST_SELECT` に `speeches` を足す | 全発言本文がペイロードに乗る。**非推奨** |

案Aを採る場合、`packages/supabase/types/supabase.types.ts` の再生成と
`CommitteeMeetingSummary` 型への `speechCount` 追加をセットでコミットする。

#### 5.6.2 選定ロジックの切り出し

外部依存のない判定・整列なので純粋関数にする（CLAUDE.md 規約・テスト必須）。

- `web/src/features/committee-minutes/shared/utils/select-featured-meetings.ts`
- `select-featured-meetings.test.ts`

引数は `CommitteeMeetingSummary[]`（`speechCount` 付き）と件数、戻り値は選定済み配列。
閾値10はこのファイル内の名前付き定数にする。

### 5.7 代表質問・一般質問から

> **前提**: PR #44（マージ済み）が `general_questions.question_type` を追加済み。

セクション見出しは **「代表質問・一般質問から」**。
最新の定例会から3件。**出典ラベル「令和◯年◯月定例会より」を必ず表示**。

| 要素 | データ源 |
|---|---|
| 質問種別 | `general_questions.question_type`（`general` \| `representative`） |
| 見出し | `general_questions.topics[0].title` |
| 議員名 | `general_questions.questioner_name` を「◯◯ 議員」表示 |
| リンク | `/questions/[id]` |

**既存ユーティリティを再利用すること。** PR #44 が
`web/src/features/general-questions/shared/utils/question-type-label.ts` に
以下を切り出している。トップ用に同等のものを作らない。

```ts
/** 質問種別の表示ラベル。未知の値が来ても一般質問として扱う */
export function questionTypeLabel(questionType: string | null): string;

/** 「代表質問・第5日・1番目」のようなカード用のメタ表記 */
export function questionMetaLabel(
  questionType: string | null,
  sessionDay: number,
  questionOrder: number
): string;
```

トップのカードは省スペースなので、`questionMetaLabel`（日・順番まで入る）ではなく
**`questionTypeLabel` だけを使い、議員名と並べて「代表質問／◯◯ 議員」と出す**のが収まりが良い。
`questionTypeLabel` は `question_type` に想定外の値が入っても一般質問として扱う
フォールバックを持つため、データ投入が途中の会期があっても安全に呼べる。

選定は当面、既存の並び順（`session_day` 昇順 → `question_order` 昇順）の先頭3件でよい。
この順序だと**代表質問が先頭に来やすい**（代表質問は会期の早い日に行われる）。
3件すべてが代表質問になるのを避けたい場合は種別を散らす選定に変えるが、
本設計では単純な先頭3件とし、将来の調整点として残す。

**ナビとバナーの文言**: PR #44 が
`web/src/components/top/general-questions-banner.tsx` の文言を
「一般質問」→「代表質問・一般質問」に変更している。
新トップでは旧バナーごと撤去されるが、**5.1節のPC横ナビのラベルは要調整**。
「代表質問・一般質問」はピルに入れるには長すぎるため、
ナビは **「質問」** の1語にするのを推奨する（6項目が横に並ぶため）。

**会期IDの取り回し**: `getLatestSessionWithQuestions()` は **slug 文字列しか返さない**。

| 案 | 内容 | 評価 |
|---|---|---|
| **A（推奨）** | `findLatestSessionSlugWithPublishedQuestions` を `{ id, slug, name }` を返すよう拡張し、`getGeneralQuestionsBySession(id)` を直接呼ぶ | DB往復1回。出典ラベル用の `name` も同時に得られる |
| B | `getCouncilSessionBySlug(slug)` を挟んで id を引く | 既存関数のみで済むが往復2回 |

案Aは `getLatestSessionWithQuestions()` の**戻り値のシェイプが変わる**。
現在この loader はキャッシュされていないが、キャッシュ化する場合は
**keyParts を `-v2` でバンプすること**（CLAUDE.md のデータキャッシュ事故の再発防止ルール）。
また現在の呼び出し元 `(main)/page.tsx` の `GeneralQuestionsBanner` も併せて直す。

### 5.8 気になるテーマから

**8テーマ**のタイル。漢字一文字の角丸アイコン + ラベル。
参照 `レイアウト変更/テーマ 8分類.dc.html`。

> **6→8に変更した理由**: 一般質問側には既にキーワード判定による**11分類**が
> 実装されている（`build-topic-groups.ts` の `CATEGORY_MAP`、キーワード183語）。
> 6テーマだと「環境・脱炭素」と「行政・県政」の行き場が無く、
> 「まち・交通」に環境が混ざるなど無理が出ていた。
> **11分類をすべてどこかに収めるため 8 にした。**

#### 5.8.1 テーマ定義と対応表

**この表が唯一の定義元。** `theme-mapping.ts` に1か所で持ち、
テーマ定義・アイコン・配色・検索語・既存分類との対応をすべてここから引く。

| テーマ | 漢字 | 一般質問（11分類） | 議案（tags） | 検索 `q` |
|---|---|---|---|---|
| 子育て・教育 | 育 | 子育て・教育 | 子育て・教育 | `子育て` |
| 福祉・医療 | 医 | 健康・医療／高齢者・福祉 | 福祉・医療 | `医療` |
| 防災・安全 | 防 | 防災・安全／警察・人権 | —（個別に付与） | `防災` |
| まち・交通 | 街 | 交通・まちづくり | まちづくり・環境（大半） | `交通` |
| 環境・エネルギー | 環 | 環境・脱炭素 | まちづくり・環境（環境分） | `環境` |
| しごと・農業 | 農 | 産業・農林水産 | —（個別に付与） | `農業` |
| 観光・文化 | 観 | 観光・地域振興／スポーツ・文化 | —（個別に付与） | `観光` |
| 県政・お金 | 県 | 行政・県政 | —（予算・条例など） | `県政` |

一般質問の11分類は**すべて上表のいずれかに収まる**（検算済み）。

> **注意1**: `CATEGORY_MAP` にはどのキーワードにも当たらない場合の
> フォールバック `その他` がある。これは8テーマのどれにも属さない。
> トップでは**「ほかのキーワードで探す」ボタン**（5.8.3節）が導線を担うが、
> これは**利用者の逃げ道であって分類上の受け皿ではない**。
> `その他` のデータがどこかのテーマに属するようになるわけではなく、
> 利用者が自分でキーワードを思いつく必要がある。
> トップは入口なのでこれで足りるが、**テーマページを作るときは
> 分類側の受け皿を別途決める**必要がある（11章の論点2）。
>
> **注意2**: 議案側のタグは現状3つ（まちづくり・環境／子育て・教育／福祉・医療）しかなく、
> 防災・しごと・観光・県政に対応するタグが**存在しない**。
> タグ付けは手動運用（`.claude/skills/bill-tagging`）なので、
> 8テーマに合わせてタグを増やす運用判断が別途必要（11章の論点2）。
> 「まちづくり・環境」は**まち・交通と環境・エネルギーに1件ずつ振り分ける**。

#### 5.8.2 配色

色は**紫・ベージュ・コーラルの3系統**を交互に当てる（黄は廃止。2.2節の原則2）。

| テーマ | 系統 | アイコン面 | アイコンの文字 |
|---|---|---|---|
| 子育て・教育 | コーラル | `pref-coral-bg` `#fde2d6` | `pref-coral-text` `#7a2f14` |
| 福祉・医療 | 紫 | `mirai-gradient-start` `#dbd3f9` | `pref-accent-deep` `#3d3868` |
| 防災・安全 | ベージュ | `mirai-surface-warm` `#eae6dd` | `pref-beige-text` `#4a4336` |
| まち・交通 | 紫 | `mirai-gradient-start` | `pref-accent-deep` |
| 環境・エネルギー | ベージュ | `mirai-surface-warm` | `pref-beige-text` |
| しごと・農業 | コーラル | `pref-coral-bg` | `pref-coral-text` |
| 観光・文化 | 紫 | `mirai-gradient-start` | `pref-accent-deep` |
| 県政・お金 | ベージュ | `mirai-surface-warm` | `pref-beige-text` |

タイル面は各系統の淡色（コーラルは `pref-coral-tint`、紫は `pref-surface-tint`、
ベージュは `pref-surface-sub`）。ラベル文字は `mirai-text`。

アイコンの文字に `pref-accent`（`#5a5591`）ではなく `pref-accent-deep`（`#3d3868`）を
使うのは、`#dbd3f9` の上では前者が 4.68:1 と余裕が乏しいため（後者は 7.48:1）。

#### 5.8.3 レイアウト

| 幅 | 配置 | アイコン | ラベル |
|---|---|---|---|
| PC | 8タイル横一列 | 56px（角丸18px） | 14px・1行 |
| スマホ | 4列×2行 | 40px（角丸13px） | 12px・2行折り返し |

**タイルの下に横長ボタン「ほかのキーワードで探す」**を置く（`/search`、`q` なし）。

| 項目 | PC | スマホ |
|---|---|---|
| 高さ | `min-height: 52px` | `min-height: 48px` |
| 文字 | 15px / weight 500 / `pref-pill-text` `#332e5e` | 14px |
| アイコン | lucide `Search` 18px | 16px |
| 形状 | `rounded-full`、枠線 1.5px、横幅いっぱい・中央寄せ | 同左 |

**枠線は `pref-accent` `#5a5591` を使う**（白比 6.68:1）。

> **参照デザインからの変更点。** デザインは枠線を `#cec8ea` にしているが、
> 白地で **1.61:1**、セクション面 `#f7f4ee` の上で **1.46:1** しかなく、
> UI部品の境界の目安 3:1 に遠く届かない。ボタンの輪郭がほぼ見えなくなるため、
> 文字色と系統を揃えて `pref-accent` に上げる。
> 文字 `#332e5e` は白比 12.40:1 で問題ない。

アイコンは**インラインSVG禁止**のため `lucide-react` の `Search` を使う
（デザインのSVGをそのまま移植しないこと）。
タップ領域は48〜52pxで、規約の最小44pxを満たす。

**「その他」タイルは作らない。** どのテーマにも入らない項目や将来の未分類データは
このボタンが受ける（5.8.1節の注意1）。

> **実装時の注意 — 8タイル横一列は 1280px 前提**。
> 参照デザインの 1280px では1タイルあたり約123pxだが、
> このリポジトリの `pc:`（**1000px**）で同じ8列を組むと約88pxまで縮み、
> 「環境・エネルギー」のような長いラベルが確実に溢れる。
> **8列は `pcl:`（1400px）以上か、最低でも1280px以上に限定し、
> `pc:`〜その間は4列×2行にする**こと。参照デザインをそのまま
> `pc:` に当てないこと。

#### 5.8.4 リンク先: `/search?q=<検索語>`【決定済み】

原案が想定する `/themes/[slug]` は未実装で、本設計でも作らない（11章の論点2）。
当面は**既存の検索ページへ飛ばす**。

現行の検索は `q` をタイトル・要約に **`ilike` で部分一致**させる方式
（`web/src/features/search/server/repositories/search-repository.ts`）。
テーマ名「子育て・教育」をそのまま渡すとほぼヒットしないため、
**タイルごとに代表キーワードを持たせる**（5.8.1節の表）。

リンク先は `theme-mapping.ts` の1か所で持ち、
**将来テーマページに切り替えるときにリンク先だけ差し替えられる**ようにする。

**テーマ見出しの補足文は「テーマで横断検索できます」。**
参照デザインの「質問・委員会・議案をまとめて見られます」は使わない。
理由は検索対象が以下の5つで、**知事会見が含まれないため**（実装を確認済み）。

`bill_contents` / `general_questions` / `budget_overviews` /
`committee_meetings` / `committee_meeting_topics`

> **任意の小改修**: 検索のスペース区切りを OR 検索にできれば、
> 検索語を複数語（例 `子育て 教育 保育`）に広げられる（11章の論点8）。
> 現状は単一文字列の部分一致なので、まず1語で運用する。
> 検索結果の質は実装後に実際の語で確認し、ヒットが極端に少ないテーマは語を調整する。

#### 5.8.5 実装

- `web/src/features/<配置先>/shared/utils/theme-mapping.ts`
- `theme-mapping.test.ts`（純粋関数のためテスト必須）

テストでは最低限、**一般質問の11分類がすべて8テーマのいずれかに写ること**を
検証する（取りこぼしの再発防止）。

テーマ体系そのものの統一（全コンテンツへのタグ付与）は11章の論点2に送る。

### 5.9 下段

**PC 3カード / スマホ 小カード2枚**の構成だが、議員カードは今回出さないため
実質 PC 2カード / スマホ 1枚になる。レイアウトは残数に応じて調整する。

| カード | 内容 | データ源 |
|---|---|---|
| 議員から探す | **今回は非表示** | 議員テーブルがDBに存在しない |
| 県のお金 | 「令和◯年度の予算をテーマでたどる」 | `getSessionsWithBudget()` を流用。`/budget/[slug]` へ |
| 議案 | 件数チップ（条例・工事の契約・購入・計画など）+「議案をぜんぶ見る」 | 5.9.1 |

**議員カードを出さない根拠**: `members` / `legislators` / `assembly_members` 系の
テーブルは `packages/supabase/types/supabase.types.ts` に存在せず、migration も無い。
議員名は `general_questions.questioner_name` などの文字列カラムに散在するだけで、
正規化されたマスタが無い。原案§4.7 も
「議員一覧ページが未実装なら、議員カードは非表示にしてよい」と許容している。

#### 5.9.1 議案の件数集計 — 審議状況（`status`）で出す【決定済み】

参照デザインの「条例 6 / 工事の契約 10 / 購入 2 / 計画 1」という区分は**議案の内容分類**で、
DBにこの情報は無い。`bills.name` から仕分ける仕組みを新たに作る必要があるため、
**既存データにある別の切り口で出すことに決定した。**

候補は `bill_type` と `status` の2つだが、**`status`（審議状況）を採用する。**

**`bill_type` を採らない理由**: 実データの分布が極端に偏っている。
`bill` / `opinion` / `resolution` / `member_bill` の4値のうち、
スクレイプ済み408件中 **`bill` が404件・`member_bill` が4件**で、
`opinion` と `resolution` は0件。会期単位で見ても最新の令和8年9月定例会は
**57件 対 1件**で、チップを並べても情報量がない。

**`status` を採る理由**: 会期の進行に応じて数字が動くため、
「更新頻度の高い情報を前に出す」という本刷新の狙いに合う。

| 会期 | status の分布 |
|---|---|
| 令和8年9月定例会（会期中） | `submitted` 56 / `approved` 2 |
| 令和8年6月定例会（閉会後） | `approved` 34 |
| 令和8年2月定例会（閉会後） | `approved` 87 |

表示は会期の状態で出し分ける。

| 状況 | 表示例 |
|---|---|
| 会期中 | 見出し「いま57件を審議中」＋チップ「審議中 56」「可決 2」 |
| 閉会後・全件同一 status | 見出し「前回の議案はすべて可決」＋チップ「可決 34」 |
| 閉会後・status が混在 | 見出し「◯件中◯件が可決」＋該当するチップを列挙 |

**ラベルは既存の `getCardStatusLabel()` を再利用する**
（`web/src/features/bills/shared/utils/bill-status.ts`）。
`submitted` / `in_committee` / `plenary_session` を「議会審議中」にまとめ、
`approved`「可決」・`rejected`「否決」・`reported`「専決処分報告」を返す関数が既にある。
**トップ用に別のラベル定義を作らないこと。**

件数集計は、既存の `countPublishedBillsByDietSession(councilSessionId, difficultyLevel)` が
会期内総数を返すのみなので新規に必要。`status` ごとの head-count を並列で叩くか、
`status` で group by する RPC を1本用意する。
`getCardStatusLabel()` が3つの status を1ラベルに畳むため、
**畳んだ後のラベル単位で合算すること**（「議会審議中」に3 status ぶんを足す）。

#### 5.9.1.1 中身が紙で届く運用への対応【決定済み】

**県議会の議案は本文が紙で配布され、スキャンして掲載している。**
そのため会期が始まってから中身が読めるようになるまでタイムラグがある。

`publish_status = "published"` だけで数えると、**会期の序盤は件数が0**になり、
いちばん見てほしい時期に議案の入口が消える。実際に本番でも
令和8年2月定例会の5件が `published` のまま `bill_contents` を持たず、
一覧の `!inner` join から漏れて画面に出ていない状態になっている。

**件数は `published` と `coming_soon` の両方で数える。**

| publish_status | 件数に含める | 詳細ページへのリンク |
|---|---|---|
| `published` | ○ | ○ |
| `coming_soon` | ○ | ×（中身がまだ無い） |
| `draft` | **×** | × |

`draft` を除くのは、議案スクレイプ（#52）で未確認のデータが大量に入るため。

中身が未掲載の議案があるときは、件数と実際に読める数の食い違いを説明する
補足文を出す（`buildComingSoonNote()`）。

- 全件が掲載待ち: 「内容は準備でき次第、順次掲載します」
- 一部だけ掲載済み: 「うち◯件の内容を掲載中（残りは順次掲載します）」

> **既存の `coming_soon` 機構を使う。** `findComingSoonBills()` /
> `getComingSoonBills()` と `ComingSoonSection`（「これから掲載される議案」）は
> 実装済みだがトップから呼ばれていない（8.2節）。
> スキャンの進捗がそのまま `coming_soon → published` の遷移になるため、
> 運用と表示が一致する。

> 参照デザインどおりの内容分類（条例／工事の契約／購入／計画）を出したくなった場合は、
> `bills.name` からの分類ロジックが別途必要になる。11章の論点6として残す。

#### 5.9.2 draft 議案の大量投入が前提になった（#49 / #52）

議案スクレイパー（#52）は10会期ぶんの議案を `docs/data/bills/*.json` として出力済みで、
これらは **`draft` で投入される前提**になっている。#49 はその前準備として
`findFeaturedBillsWithContents` に `publish_status = "published"` を追加し、
draft の議案が `is_featured` を立てられてもトップに露出しないようにした。

件数集計では **`draft` を必ず除外すること**（`published` と `coming_soon` は含める。
5.9.1.1節）。除外を忘れると、未確認のスクレイプ済み議案が件数に乗る。

なお新デザインでは「注目の議案」セクション自体をトップから外すため、
この #49 が守っている経路（`getFeaturedBills`）は最終的にトップから呼ばれなくなる。
議案一覧ページへ移設する際にこのフィルタを落とさないよう注意する（9章 PR7）。

### 5.10 フッター注記

「非公式サイトです。AIで整理した内容を含むため、正確な情報は公式資料でご確認ください。」

既存の免責・About・FAQ・利用規約・プライバシーポリシー・GitHub リンクは
現行どおり残す（トップ下部または `Footer`）。

---

## 6. 新規に必要なデータ項目（AI生成）

**本設計書のスコープ外。別フェーズ・別PRで実施する。**
5章の各セクションはすべて未生成時のフォールバックを定義済みなので、
これが無くてもトップページは成立する。

| 対象 | 項目 | 形式 |
|---|---|---|
| 知事会見の各トピック | `headline_short` | 全角20字前後。問いかけ or 言い切り |
| 委員会の各会議 | `headline_question` | 代表議題を問いかけにしたもの、全角25字以内 |
| 委員会の各会議 | `highlight` | 最も目を引く数値を含む短句（任意・null可）、全角20字以内 |
| 委員会の各会議 | `summary_short` | 1〜2文、全角80字程度。報告内容＋委員の主な要望 |
| 一般質問 | `headline_short` | 既存タイトル流用可。数字があれば含める |

言い換えの例:

- 「一般社団法人九州観光機構の事業について」→「九州観光機構、今年は何をする？」
- 「中東情勢に伴う本県農林水産業への影響について」→「中東情勢、県の農業・漁業への影響は？」

プロンプトには **元の要約に書かれていない情報を足さない**ことを明示する。

実施時は CLAUDE.md の「AI生成コンテンツのDB更新ルール」に従うこと。
特に **生成内容を必ずユーザーに提示して確認を取ってから DB に反映**し、
提示時には日本語の自然さ・情報の整合性・**中国漢字（「议」「务」「该」等）の混入**・
答弁者フォーマットを自己レビューする。

---

## 7. フォールバック方針（まとめ）

AI生成項目が未整備の状態でもトップが成立することを設計の前提とする。

| 要素 | 本来 | フォールバック |
|---|---|---|
| 会見の話題 | `headline_short` | `press_conference_items.title` をそのまま |
| 委員会の見出し | `headline_question` | `committee_meeting_topics[0].title` |
| 委員会の要約 | `summary_short` | `committee_meetings.summary` の切り詰め |
| 委員会の数字チップ | `highlight` | **要素ごと非表示** |
| 次の定例会ピル | 日程データ | **ピルごと非表示** |
| 議員カード | 議員テーブル | **カードごと非表示** |

**空欄やプレースホルダ（`[開会日]` など）を出さないこと。** 参照デザインの
`[開会日]` はプレースホルダ表記であり、そのまま実装してはならない。

---

## 8. 付随して直す／整理する項目

刷新に伴って触る範囲にあり、まとめて直しておくべきもの。

### 8.1 `unstable_cache` の keyParts 重複（バグではないが紛らわしい）

`getFeaturedBills`（`get-featured-bills.ts` 55行目）と
`getBillsByFeaturedTags`（`get-bills-by-featured-tags.ts` 99行目）が
**どちらも keyParts `["featured-bills-list"]`** を使っている。
引数シグネチャも同一 `(DifficultyLevelEnum, string | null)` で、
戻り型は `BillWithContent[]` と `BillsByTag[]` で非互換。

**ただしキャッシュキーは衝突しない。** Next.js 15.5.9 の実装では、
キーに**コールバックのソース文字列が含まれる**ため。

```js
// next/dist/server/web/spec-extension/unstable-cache.js
const fixedKey = `${cb.toString()}-${Array.isArray(keyParts) && keyParts.join(',')}`;
const invocationKey = `${fixedKey}-${JSON.stringify(args)}`;
```

2つの loader は関数本体が異なるので `cb.toString()` が異なり、別のキーになる。
`tags` も両方 `[CACHE_TAGS.BILLS]` なので、タグによる再検証の挙動も同じ。
**実害は無い。**

とはいえ、keyParts は「このキャッシュが何か」を人が読むための識別子であり、
同じ名前が2つあると調査時に紛らわしい。**片方を別名に変えることを推奨する**
（例：`["bills-by-featured-tags"]`）。

> **注意**: keyParts を変えると**キャッシュキーが変わり、既存のキャッシュが無効化される**。
> 戻り値のシェイプを変えるわけではないので安全だが、
> デプロイ直後は一時的にキャッシュミスが増える。

### 8.2 未使用コードの整理

| 対象 | 状況 |
|---|---|
| `web/src/components/layouts/desktop-menu/`（8ファイル） | どこからも import されていない |
| `web/src/components/top/coming-soon-section.tsx` | `page.tsx` から import されていない（`loadHomeData` は `comingSoonBills` を返すが捨てられている） |
| `(main)/page.tsx` の `activeSession` | `getActiveCouncilSession()` で取得するが JSX で未使用 |

### 8.3 `<main>` の二重ネスト

`(main)/layout.tsx` が `<main>` を持つのに、`(main)/page.tsx` の内側でも
`<main className="flex flex-col gap-16">` を使っている。刷新時に解消する。

### 8.4 キャッシュタグの追加

press-conferences / committee-minutes 系の loader には `unstable_cache` が無く、
専用のキャッシュタグも未定義。新トップでこの2つが前面に出るため、
`web/src/lib/cache-tags.ts` に `PRESS_CONFERENCES` / `COMMITTEE_MEETINGS` を追加し、
loader をキャッシュ化する。

> **必須**: 戻り値のシェイプを変える loader は `keyParts` を `...-v2` でバンプすること。
> Vercel のデータキャッシュはデプロイをまたいで残り、旧シェイプが返って
> 本番がクラッシュした実績がある。受け取り側も防御的に書く。

### 8.5 `viewport.maximumScale: 1`

`web/src/app/layout.tsx` 66行目がピンチズームを抑止しており、
アクセシビリティ上の課題（原案§9の「コントラスト比 4.5:1 以上」と同じ文脈）。
文字サイズ拡大モードは実装済みだが `html.large-text main .text-*` が
**`main` 配下のみ**を対象としているため、新設するボトムナビ・ヘッダーのラベルには効かない。
今回この2点を直すかは実装時に判断する。

---

## 9. 実装ステップ（PR分割案）

ベースブランチは**すべて `fukuoka-pref/develop`**。各PRで
`pnpm lint` / `pnpm typecheck` / `pnpm test` を push 前にローカル実行する。

配色・会期中モードとも確定したため、**全PRが着手可能**。

| # | 内容 | 主な変更ファイル |
|---|---|---|
| 1 | データ層 | `speech_count` migration + 型再生成、`findRecentPressConferenceDates`、`findCouncilSessionsByYear`、**会期日程（`start_date`/`end_date`）のDB投入経路（5.5.2節）**、`status` 件数集計（`draft` 除外・`coming_soon` 込み）、純粋関数3本 + テスト |
| 2 | トークン + フォント | `globals.css`（`pref-*` トークン追加、`--font-rounded` 追加、`--font-lexend` と `.font-lexend` 削除）、`app/layout.tsx`（Zen Maru Gothic 700 追加、Lexend Giga 削除、`themeColor` を `#a495d6` に、`NextTopLoader` を `#5a5591` に） |
| 3 | レイアウト幅とナビ | `page-layout-utils.ts`（`isWidePage` に `/`）、`header-client.tsx`（PC横ナビ）、ボトムナビ新規、`(main)/layout.tsx`（下余白） |
| 4 | ヒーロー（閉会中）+ 知事会見カード | 新 feature コンポーネント、`announcement-item.tsx` / `qa-item.tsx` へのアンカー付与 |
| 5 | **会期中モード 段階1** | 会期状態の分岐（`closed \| in_session`）、ピル・コピーの差し替え、定例会の帯の強調ルール（5.3.2節） |
| 6 | 残りのセクション | 定例会の帯 / 委員会カード / 代表質問・一般質問 / テーマ / 下段 / フッター |
| 7 | 旧トップの撤去 | `Hero`・`CurrentCouncilSession`・バナー群を削除、議案のテーマ別一覧を議案一覧ページへ移設 |
| 8 | **会期中モード 段階2** | ヒーローを定例会に差し替え、進み具合バー、会期の流れ、入口タイル2枚。必要なら `parseSessionSchedule()` を拡張（5.3.3節） |
| 9 | AI生成項目 | 別フェーズ（6章） |

**PR5（会期中モード 段階1）は公開前に必ず入れること。**
これが無いまま公開すると、会期中に「いまは閉会中」と誤表示される。
PR8（段階2）は公開後でもよい。

PR1 は会期日程のDB投入を含み、これが入らないと定例会の帯（5.5節）も
会期中モード（5.3.2節）も作れないため、最初に片付ける。

純粋関数（`resolve-session-slot-status` / `select-featured-meetings` / `theme-mapping`）は
**PR1 でテストとセットで入れる**。UIより先に確定させることで、PR6 の実装が軽くなる。

各PRで `/dev` にプレビューページを追加する:
`web/src/app/dev/_lib/registry.ts` にエントリを足し、
`ComponentShowcase` / `PreviewSection` でラップする
（詳細は [docs/20260301_0700_開発用UIプレビュー環境.md](../20260301_0700_開発用UIプレビュー環境.md)）。

### 9.1 実装時に守る規約（CLAUDE.md より）

- **インラインカラーコード禁止**。`text-[#xxx]` 等は使わず、`globals.css` の
  トークンを使う。新色は先にトークンを追加する。
- **インラインSVG禁止**。アイコンは `lucide-react` から import する。
- **生 `<button>` 禁止**。`@/components/ui/button` の `Button` を使う。
- **`h-screen` / `min-h-screen` 禁止**。`h-dvh` / `min-h-dvh` を使う。
- 本文テキストは `text-xs` / `text-sm` / `text-base` を使う
  （文字サイズ拡大モードが効くのはこの3つだけ）。
- 外部依存のない計算・変換・判定は `utils/` に純粋関数として切り出し、
  同階層に `*.test.ts` を必ず置く。
- Biome: 2スペース・LF・ダブルクォート・セミコロン・80文字幅。

---

## 10. 受け入れ基準

- [ ] PC 1280px / スマホ 390px で参照デザインと同じ構成・順序になっている。
- [ ] 700〜1000px の中間幅でレイアウトが破綻しない（横スクロールが出ない）。
- [ ] ヒーローに最新の知事会見が表示される。
- [ ] **会期中・閉会中の両方で表示が破綻しない**（5.3.2節）。
      会期中は「いま定例会中」、定例会の帯の該当回が `いま開会中` になる。
- [ ] **強調枠が常に1つだけ**になっている（会期中に `いま開会中` と `つぎはここ` が
      並ばない。5.5節）。
- [ ] 紫のベタ塗り＋白文字が使われていない（2.2節の原則1）。
- [ ] 黄色（`#ffe7a8` 等）が使われていない（2.2節の原則2）。
- [ ] テーマタイルが**8枚**あり、`pc:`（1000px）付近でラベルが溢れていない（5.8.3節）。
- [ ] タイルの下に「ほかのキーワードで探す」ボタンがあり、`/search` に遷移して
      検索フォームが表示される（5.8.3節）。枠線が視認できる。
- [ ] 「その他」タイルが存在しない。
- [ ] `theme-mapping.test.ts` で**一般質問の11分類がすべて8テーマに写る**ことを
      検証している（5.8.5節）。
- [ ] 「会期の流れ」の**採決日と閉会日が別の日として正しく出ている**（5.3.3節）。
      参照デザインの「10月16日 採決・閉会」をそのまま実装していない。
- [ ] 定例会の帯に**臨時会が混ざらない**（5.5.1節）。
- [ ] 議案の件数に `draft` が乗らない（`published` + `coming_soon` で数える。5.9.1.1節）。
- [ ] 中身が未掲載の議案があるとき、件数との食い違いを説明する補足文が出る。
- [ ] 会期の序盤で全件が掲載待ちでも、議案の件数タイルが消えない。
- [ ] 委員会カードに、質疑なし・発言数10未満の回が出ない。
- [ ] 数字チップ・次の定例会ピル・議員カードなど、**データが無い要素は
      空欄やプレースホルダを出さず非表示**になる。`[開会日]` が画面に出ない。
- [ ] すべてのカード・リンクが既存の詳細ページに遷移する（404が無い）。
- [ ] 補足テキストを含め、文字と背景のコントラスト比が 4.5:1 以上
      （2.3節の「禁止する組み合わせ」が使われていない）。
- [ ] スマホでボトムナビがコンテンツを隠さない。`env(safe-area-inset-bottom)` が効いている。
- [ ] PCでボトムナビが出ず、スマホで横並びナビが出ない。
- [ ] 既存ページ（議案・予算・事務事業評価・委員会・記者会見）の見た目が変わっていない。
- [ ] `siteConfig.features.aiChat` を一時的に `true` にしても、
      トップの本文とチャットサイドバーが重ならない（4.2節）。
- [ ] `pnpm lint` / `pnpm typecheck` / `pnpm test` が通る。
- [ ] 新規の純粋関数すべてに `*.test.ts` がある。

---

## 11. 未決事項・将来の論点

1. ~~**次の定例会の日程データの持ち方**~~ → **#52 で解消**。
   `parseSessionSchedule()` がスクレイピングで開会日・閉会日を取得する（5.5.1節）。
   残る作業は**DBへの投入経路の追加**のみで、これは論点ではなく実装タスク（5.5.2節・9章PR1）。
2. **テーマ体系の統一**。8テーマと既存カテゴリの**対応表は確定した**（5.8.1節）ので、
   トップのタイルはこれで作れる。残る論点は次の3つ。

   - **議案のタグが3つしかない。** 防災・しごと・観光・県政に対応するタグが無く、
     8テーマのうち4つは議案側が空になる。タグ付けは手動運用
     （`.claude/skills/bill-tagging`）なので、タグを増やす運用判断が要る。
   - **委員会と知事会見には分類が一切無い。** テーマページを作るなら、
     この2つにもテーマを付ける必要がある。
   - **`その他` の分類上の受け皿**（5.8.1節の注意1）。トップは
     「ほかのキーワードで探す」ボタンで導線を確保したが、これは逃げ道であり
     分類の解決ではない。テーマページでは未分類データの置き場を決める必要がある。

   全コンテンツにテーマを付ける方式は、既存の `tags` / `bills_tags` を拡張して
   他テーブルにも中間テーブルを足すか、テーマ専用のテーブルを新設するかの
   設計判断になる。**テーマページ（`/themes/[slug]`）の前提条件。**
3. **議員紹介の扱い**。県議会では会派バランスへの配慮が必要。
   議員マスタを新設するか、当面は入口（一覧リンク）のみにするか。
4. ~~**会期中モード（未デザイン）**~~ → **2026-09-24 にデザインが届き解消**（5.3.2節）。
   段階1（文言差し替え）／段階2（ヒーロー差し替え）の2段階で実装する。
   **段階1は公開に必須。** 令和8年9月定例会が 2026-09-09〜10-16 で開催中のため、
   閉会中だけ実装すると「いまは閉会中」という誤表示になる。

   残る実装上の論点は**「会期の流れ」4ステップの日程をどこまで出すか**（5.3.3節）。
   会期日程ページには必要な情報が揃っているが、`parseSessionSchedule()` は
   開会日・閉会日しか抽出していない。パーサーを拡張するか、
   行ごと非表示のフォールバックで済ませるかを段階2の着手時に決める。
5. ~~**サイト全体の紫 → ティール移行の是非**~~ → **不要になった**。
   配色を薄紫系に確定したため、トップと既存ページの系統が揃う（2.4節）。
   `pref-accent`（`#5a5591`）は既存 `--primary`（`#a495d6`）の同系色。
6. ~~**議案の件数チップの区分**~~ → **決着**。審議状況（`status`）で出す（5.9.1節）。
   参照デザインどおりの内容分類（条例／工事の契約／購入／計画）を出したくなった場合は、
   `bills.name` からの分類ロジックが別途必要になる。将来の拡張候補として残す。
7. ~~**配色の最終決定**~~ → **決着**。薄紫系（2章）。
8. **検索のOR対応（任意の小改修）**。現行の検索は `q` の単一文字列を `ilike` で
   部分一致させる方式。スペース区切りをOR検索にできれば、テーマタイルの検索語を
   複数語に広げてヒット率を上げられる（5.8節）。トップ刷新とは独立して進められる。
